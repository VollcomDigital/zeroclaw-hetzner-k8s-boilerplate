apiVersion: v1
kind: ConfigMap
metadata:
  name: zeroclaw-config
  namespace: zeroclaw
  labels:
    app.kubernetes.io/name: zeroclaw-assistant
    app.kubernetes.io/component: ai-assistant
    app.kubernetes.io/part-of: zeroclaw
data:
  # LiteLLM AI Gateway endpoint — all LLM traffic from ZeroClaw is
  # routed through this internal gateway for cost tracking, rate
  # limiting, and model routing.
  litellm_gateway_url: "https://ai-gateway.yourcompany.com"
