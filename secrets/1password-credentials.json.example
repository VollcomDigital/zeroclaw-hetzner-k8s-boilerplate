{
  "note": "Replace this file with a real 1Password Connect credentials bundle from `op connect provisioner` (see secrets/README.md). This placeholder is only for Docker bind-mount checks; Connect will not work until you replace it."
}
